package com.example.amazonclone.base

import androidx.appcompat.app.AppCompatActivity
import com.example.amazonclone.utils.Injectable

open class BaseActivity : AppCompatActivity(), Injectable {
    // Common base class for activities
}